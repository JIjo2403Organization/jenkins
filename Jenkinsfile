pipeline {
    agent any

    stages {
        stage('Read Text File') {
            steps {
                bat 'echo Jenkins Pipeline Running'
                bat 'type text_file.txt'
            }
        }
    }
}
